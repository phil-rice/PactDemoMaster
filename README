This is the git repo that is used to build the different microservices that are part of the PactDemo

Eventually this will be pushed to three Heroku servers with an environment variable that controls which
batch file is to be executed.


# Making this work with Git

This uses Git submodules. To check it out do the following:

* git clone xxx
* go into the directory
* git submodule init
* git submodule update
